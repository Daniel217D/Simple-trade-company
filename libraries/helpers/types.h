#pragma once

namespace Cmp {
    enum dayTime {
        morning, day, evening
    };

    enum sellerType {
        storage, shop
    };

    enum clientType {
        wholesale, retail
    };

    enum productType {
        apple = 0, pear = 1, carrot = 2
    };
}